import ShowPath from "../../components/ShowPath";

export const CreateAuctionPage = () => {
    return (
        <div>
                <ShowPath />
            Copy stuff from crist.
        </div>
    );
}